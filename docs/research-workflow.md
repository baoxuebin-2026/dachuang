# 本项目研究工作流（由建模工作流改编）

记录日期：2026-09-24。参考仓库 [math-modeling-ai-workflow](https://github.com/baoxuebin-2026/math-modeling-ai-workflow/tree/9263c3335ac88b8cbb09bbada5cdea444ea16ed1) 的代码、预处理、建模、验证、图表与证据规范。上游为 MIT 许可；本文件为本项目的研究约定，并非复制竞赛任务书或论文模板。

## 阶段决策

阶段 0—10 按项目组已约定的研究顺序推进。每进入影响研究路线的阶段，先核查材料、提出备选与推荐、提交阶段报告；待项目组确定后开展该阶段的研究。2026-09-24 已确认阶段 2：**UCI 309 主实验、UCI 487 独立辅助实验、UCI 322 备用**；阶段 3 已完成 EDA，阶段 4 已确认 S1 模拟处置等级。阶段 5 已批准 [M1 的 A/B 基线协议](stage5-m1-experiment-protocol-proposal.md)并完成 [UCI 309 实验结果记录](stage5-m1-pilot-results.md)；仅支持风洞实验室条件下的响应检测。487 独立辅助研究与模拟人员场景仍待下一决策。

## 数据和陈述的边界

- `data/raw/` 保留原包、文件名、来源网址、版本／访问日期、SHA-256；不改写原始文件，也不提交原始大文件。解析后写入 `data/processed/` 并记录参数、单位、采样率和排除规则。
- 公开数据标注为**实验室实测**，模拟人员轨迹标注为**仿真**；两类数据的来源、时间轴、验证用途分别保存。309 与 487 是不同实验，不作同一现场、同步同位置的联合观测。
- UCI 309 的释放程序只提供“释放开始及其后检出”的实验参照；如要声称事故前预警或四级安全风险，应在后续阶段另有可审查的真值依据。
- D435、Jetson、环境传感器写作**拟部署架构**；未有实机采集或测试时，不报告设备实测性能。未完成的烟雾／积液／视觉验证及发表目标参见 [承诺对照](commitments.md)。

## 代码和计算约定

1. 经人类确定指标和实验方案后，按研究任务组织 `src/`：独立的读取与校验、预处理、实验计算、可视化模块。计算脚本输出带字段和单位的结构化表到 `results/`；可视化脚本只从正式结果读数，不重新计算结论。EDA 输出只标记 `diagnostic_only`。
2. 每次正式运行记录数据包及哈希、脚本版本／Git commit、环境和依赖、随机种子、命令／参数、输出路径、成功状态和运行 ID；派生结果在 `registries/run_record.csv`、`registries/result_registry.csv` 登记。候选阈值、模型和切分未经阶段确认，不能写为最终结果。
3. UCI 309 以独立实验文件为切分单位，严禁同一文件的时间窗口跨训练和测试；487 独立分析，记录按实验日划分。归一化、特征选择和阈值拟合仅使用训练部分；仿真记录生成机制与种子。
4. 预处理先核对字段、单位、时间零点、缺失、重复、非数、饱和、采样率、传感器漂移，再建议删除／填补／重采样规则。会改变事件时间、标签或信息泄漏的规则须在使用前交项目组决定。
5. 可解释基线优先。仅在其具体不足有证据时比较更复杂方法；报告实验单位、误报定义、检测延迟定义、分组泛化和对照／敏感性。每项比较只支持其具体试验条件。
6. 一旦正式结果修订，生成新结果 ID，在旧行写 `superseded_by` 并更新引用，保留历史记录；未经验证的输出和终端日志不可直接引用为论文结论。

## 图表和论文约定

- 先问图要证明什么：每张正文图对应一个可核查的结论、数据来源／运行 ID、图注和正文位置；正式图登记 `registries/figure_evidence.csv`。精确且简短的数字优先表格。
- 绘图程序读取正式计算结果；建议导出 SVG 与 600 DPI PNG。设置中文字体及负号、坐标和单位，统一克制配色；图内不放论文式标题，论文中写图注。时间轴区分真实采样时间和相对释放时间；实验曲线与仿真曲线在图注、图例和文件名明确区分。
- 结果图应标出与结论有关的事件、基线、区间和不确定性；不截断坐标制造差异，不使用无意义三维／装饰图。论文目标插入尺寸下检查字迹、重叠、裁切、颜色辨别，并登记人工检查结论。
- 正文图表和数字在 `registries/claim_ledger.csv` 关联运行、结果及适用边界。没有证据链的结论只能写作待验证假设；复合情境的仿真表现不可当作真实焦化厂事故统计。

## 上游规则的适用范围

沿用上游的“先预处理检查、再选择模型”“计算与绘图分离”“运行—数字—图片—文字可追溯”“图表最终尺寸核查”。原仓库的 72 小时竞赛日程、C 题／分问 qX 目录、竞赛评审等级、国一目标和竞赛提交／匿名要求均不适用本两年期大创项目。引用上游代码时再分别检查其许可证和项目内具体适配；不直接复制赛题示例数值。
